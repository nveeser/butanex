variant: fcos
version: 1.5.0

passwd:
  users:
    - name: user1
      ssh_authorized_keys:
        - key1

storage:
  files:
    - path: /opt/file2
      contents:
         local: host-dir/input-file.txt
